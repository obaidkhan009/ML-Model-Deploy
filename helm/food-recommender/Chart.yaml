# Helm Chart metadata
apiVersion: v2
name: food-recommender
description: A Helm chart for deploying the Food Recommendation ML API on Kubernetes
type: application
version: 1.0.0       # Chart version
appVersion: "1.0.0"   # Application version (matches your Docker image tag)
